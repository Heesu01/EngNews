import styled from "styled-components";

import Banner from "../components/Banner";

const Main = () => {
  return (
    <Container>
      <Banner />
    </Container>
  );
};

const Container = styled.div``;

export default Main;
